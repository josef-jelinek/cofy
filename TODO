Planned features

to do:
  - regular expressions
  - static environment analysis -> syntax macros
  - a way for iteration (either trampoline or recur syntax)
  - running scripts from application/x-cofy script tags
  - cofy function library

longer-term to do:
  - persistent data structures
  - compile mode using eval (after benchmarking)
  - server-side compiler to javascript
